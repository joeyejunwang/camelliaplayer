name: Build Windows

on:
  push:
    tags:
      - '**'

jobs:
  build-windows:
    name: Build Windows (release)
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Show Flutter & Dart versions
        run: |
          flutter --version
          dart --version

      - name: Verify Flutter Windows toolchain
        run: flutter doctor -v

      - name: Get pub dependencies
        run: flutter pub get

      - name: Analyze project
        run: flutter analyze --no-fatal-warnings

      - name: Build Windows (release)
        run: flutter build windows --release

      # Bundle the EXE, DLLs, and data assets into a single zip archive
      - name: Compress Windows Release
        run: |
          Compress-Archive -Path build/windows/x64/runner/Release/* -DestinationPath camellia-player-windows-release.zip

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: camellia-player-windows-release
          path: camellia-player-windows-release.zip
          if-no-files-found: error
          retention-days: 14

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: camellia-player-windows-release.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}